// soundcloud api keys go here

// CLIENT ID HERE
export const CLIENT_ID = 'REPLACE_ME_WITH_YOUR_CLIENT_ID'; 

// Redirect URL when using soundcloud's auth(if we use it at all) should redirect back to our site
export const REDIRECT_URI = `${window.location.protocol}//${window.location.host}/callback`;